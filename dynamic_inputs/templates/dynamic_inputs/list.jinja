{% extends "dynamic_inputs/base.jinja" %}
{% block content %}
    <div class="list-group">
        {% for entry in entrys %}
            <a href="{{ entry.get_absolute_url() }}"
               class="list-group-item list-group-item-action">{{ entry.id }}</a>
        {% endfor %}
    </div>
{% endblock content %}
